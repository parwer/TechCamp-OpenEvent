version: '3'
services:
  app:
    image: techcamp-openevent2-app:latest
    ports:
      - "9069:3000"
    
    deploy:
      replicas: 1
    restart: unless-stopped

    environment:
      NODE_ENV: production